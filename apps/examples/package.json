{
  "name": "@aspect/examples",
  "version": "3.15.0",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "build:web": "npm run build",
    "dev": "serve . -l 8080 --no-clipboard",
    "dev:electron": "npm run build && electron electron/main.cjs",
    "build:desktop": "npm run build && electron-builder",
    "build:mobile": "npm run build && node scripts/cap-sync.mjs && npx cap sync",
    "run:android": "npm run build:mobile && npx cap run android",
    "run:ios": "npm run build:mobile && npx cap run ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@aspect/cocos2d": "*",
    "@aspect/extensions": "*",
    "@aspect/external": "*",
    "@aspect/socketio": "*",
    "@aspect/tilemap": "*"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  },
  "build": {
    "appId": "org.cocos2d.examples",
    "productName": "Cocos2d Examples",
    "directories": {
      "output": "dist-desktop"
    },
    "files": [
      "electron/**",
      "dist/**",
      "index.html",
      "style*.css",
      "res/**"
    ],
    "win":   { "target": "portable" },
    "mac":   { "target": "dmg" },
    "linux": { "target": "AppImage" }
  }
}
